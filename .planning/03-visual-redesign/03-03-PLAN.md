---
phase: 03-visual-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified: []
autonomous: false
requirements:
  - THEME-01
  - THEME-02

must_haves:
  truths:
    - "All 13 slides display correctly in browser with dark background and no white bleed-through"
    - "The production build succeeds without errors"
    - "The deployed GitHub Pages site at https://pigotz.github.io/drum-theory/ renders the dark theme correctly"
  artifacts:
    - path: "dist/"
      provides: "Production build output for GitHub Pages deployment"
  key_links:
    - from: "dist/"
      to: "https://pigotz.github.io/drum-theory/"
      via: "git push to gh-pages branch (manual deploy)"
      pattern: "gh-pages"
---

<objective>
Visual QA pass across all 13 slides followed by production build and manual deploy to GitHub Pages.

Purpose: Confirm the dark theme, typography, component restyles, and VexFlow changes all render correctly in both development and production. The build pipeline must not strip or override any CSS. GitHub Pages must serve the dark theme.

Output: Verified presentation running on https://pigotz.github.io/drum-theory/ with the dark aesthetic.
</objective>

<execution_context>
@/home/pigotz/Projects/drum-theory/.claude/get-shit-done/workflows/execute-plan.md
@/home/pigotz/Projects/drum-theory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-CONTEXT.md
@/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-UI-SPEC.md
@/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-01-SUMMARY.md
@/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build production bundle</name>
  <files></files>
  <read_first>
    - `/home/pigotz/Projects/drum-theory/package.json` — confirm the build command and base URL configuration
    - `/home/pigotz/Projects/drum-theory/slides.md` — lines 1–11, confirm `base: /drum-theory/` is still present and `theme: seriph` is absent
  </read_first>
  <action>
Run the production build from the project root:

```bash
cd /home/pigotz/Projects/drum-theory && npm run build
```

The build output goes to `dist/`. If the build fails:
- Check for TypeScript errors in `DrumStaff.vue` or `LoopPlayer.vue` first
- Check that `style.css` is valid CSS (no syntax errors)
- Check that `slides.md` frontmatter is valid YAML (no broken keys after theme removal)

After build succeeds, verify the output CSS includes the dark theme tokens:

```bash
grep -r "#0f1117\|Space.Grotesk\|slidev-theme-primary" /home/pigotz/Projects/drum-theory/dist/ | head -5
```
  </action>
  <verify>
    <automated>cd /home/pigotz/Projects/drum-theory && npm run build 2>&1 | tail -5 && test -f dist/index.html && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0 (no errors)
    - `dist/` directory exists and contains `index.html`
    - `grep -r "Space" /home/pigotz/Projects/drum-theory/dist/` returns at least one match (Space Grotesk reference survived the build)
    - No error output from the build command mentioning `slides.md`, `style.css`, `DrumStaff.vue`, or `LoopPlayer.vue`
  </acceptance_criteria>
  <done>Production build succeeds; dist/ contains deployable output with dark theme CSS included.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Plans 01 and 02 applied the full dark visual redesign:
- `style.css`: dark background (#0f1117), Space Grotesk font, groove grid cell borders, two-cols right panel surface color, drum-sheet blend mode
- `slides.md`: `theme: seriph` removed
- `DrumStaff.vue`: VexFlow context colors set to #e2e8f0 before stave draw
- `LoopPlayer.vue`: button restyled (white/gray states), emoji removed, onSlideLeave cleanup added

Task 1 of this plan ran `npm run build` successfully.
  </what-built>
  <how-to-verify>
Start the dev server: `cd /home/pigotz/Projects/drum-theory && npx slidev slides.md`

Navigate ALL 13 slides and check each item:

**Slide 1 (Cover):** Dark (#0f1117) background. Heading "You Can't Read This" uses Space Grotesk, color #e2e8f0. No white bleed.

**Slide 2 (Drum sheet):** drum-sheet.png blends against dark background (white areas of the PNG should not appear as harsh white rectangles — they should be transparent or muted). No white background box visible behind the image.

**Slides 3–7 (Note durations):** SVG symbols (whole-note.svg, etc.) are visible as white/light shapes on dark background. Right panel has #1a1d27 surface color (slightly lighter than slide background). Headings ("Whole Note", "Half Note", etc.) use Space Grotesk.

**Slide 8 (Key insight):** Dark background. 48px bold text visible and readable.

**Slide 9 (Staff reading):** Left panel has heading + colored list. Right panel shows DrumStaff VexFlow notation — stave lines, clef, and time signature must be visible (#e2e8f0). Kick noteheads red (#ef4444), snare blue (#3b82f6), hi-hat yellow (#eab308).

**Slide 10 (Quote / statement):** Dark background. Quote text visible.

**Slide 11 (Your Roles Tonight):** Role cards retain their instrument colors (red/blue/yellow backgrounds). Text on cards legible.

**Slide 12 (Human Drum / groove grid):** Empty table cells have dark fill (#1a1d27) and visible border lines. LoopPlayer button is off-white (#e2e8f0) with dark text, label "Play" (no emoji). Click it — button switches to gray (#4b5563) with light text, label "Stop". Navigate away from slide 12 with audio playing — audio stops. Return to slide 12 — button shows "Play" again.

**Slide 13 (Outro / end):** Dark background. No white bleed.

**Global:** No slide shows a white background at any point. Font is consistently Space Grotesk throughout.
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 slides pass. Describe any issues found (e.g., "slide 9 stave still invisible", "slide 2 PNG has white box behind it").</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy to GitHub Pages</name>
  <files></files>
  <read_first>
    - `/home/pigotz/Projects/drum-theory/package.json` — confirm the deploy command (look for `deploy` script)
  </read_first>
  <action>
Run the deploy command from the project root:

```bash
cd /home/pigotz/Projects/drum-theory && npm run deploy
```

This pushes the `dist/` output to the `gh-pages` branch. The `base: /drum-theory/` in `slides.md` frontmatter ensures all asset paths resolve correctly under the GitHub Pages subdirectory.

After the command completes (typically takes 1–2 minutes for GitHub Pages to rebuild), verify the live URL responds with the new content.
  </action>
  <verify>
    <automated>cd /home/pigotz/Projects/drum-theory && git log gh-pages --oneline -3 2>/dev/null || git log origin/gh-pages --oneline -3 2>/dev/null || echo "gh-pages branch updated (check remotely)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run deploy` exits with code 0
    - No error output from the deploy command
    - The gh-pages branch is updated (git log shows a new commit if you inspect the gh-pages branch)
  </acceptance_criteria>
  <done>gh-pages branch updated; GitHub Pages rebuild triggered.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Production build deployed to GitHub Pages at https://pigotz.github.io/drum-theory/
  </what-built>
  <how-to-verify>
1. Open https://pigotz.github.io/drum-theory/ in a browser (allow 1–2 minutes for GitHub Pages to rebuild after deploy).
2. Verify slide 1 (Cover) has the dark background — NOT the old light seriph theme.
3. Navigate to slide 9 and verify the VexFlow drum stave is visible with colored noteheads.
4. Navigate to slide 12 and verify the groove grid has visible cell borders and the LoopPlayer button is styled correctly.
5. Confirm no white flash or background bleed on any slide.
  </how-to-verify>
  <resume-signal>Type "approved" if the deployed site looks correct. Type "issues" followed by description if something looks wrong on the live site but was correct in dev.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dist/ → GitHub Pages | Static file deployment; no secrets in output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | GitHub Pages deployment | accept | Presentation is public by design; no credentials or PII in dist/ |
| T-03-06 | Tampering | gh-pages branch | accept | Presentation-only; force push to gh-pages is standard practice and expected for static site deploys |
</threat_model>

<verification>
Phase 3 is complete when:
1. Production build succeeds (Task 1)
2. Visual QA passes on all 13 slides in dev (checkpoint)
3. GitHub Pages deployment succeeds (Task 3)
4. Live site at https://pigotz.github.io/drum-theory/ shows the dark theme (final checkpoint)
</verification>

<success_criteria>
- `npm run build` exits 0 with no errors
- All 13 slides display dark background in development server
- VexFlow notation visible on slide 9 with correct per-instrument colors
- LoopPlayer button styled correctly on slide 12; audio stops on slide leave
- Live site at https://pigotz.github.io/drum-theory/ reflects the dark theme
</success_criteria>

<output>
After completion, create `/home/pigotz/Projects/drum-theory/.planning/phases/03-visual-redesign/03-03-SUMMARY.md`
</output>
