---
phase: 03-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - style.css
  - slides.md
autonomous: true
requirements:
  - THEME-01
  - THEME-02
  - SVG-01
  - SVG-02
  - TYPE-01
  - TYPE-02
  - COL-01
  - COL-02
  - LAY-01
  - LAY-02

must_haves:
  truths:
    - "All 13 slides show a dark (#0f1117) background with no white bleed-through"
    - "Slide headings render in Space Grotesk font, bold, color #e2e8f0"
    - "SVG note symbols (slides 3–7) are visible (white fill shows on dark background)"
    - "Groove grid empty cells have a dark fill (#1a1d27) and visible borders (#2d3244)"
    - "Two-cols right panels show surface color #1a1d27"
    - "drum-sheet.png on slide 2 has mix-blend-mode: multiply applied"
    - "No `theme: seriph` line exists in slides.md frontmatter"
  artifacts:
    - path: "style.css"
      provides: "Global dark theme CSS — background, typography, custom properties"
      contains: "--slidev-theme-primary"
    - path: "slides.md"
      provides: "Presentation source — seriph removed from frontmatter"
      does_not_contain: "theme: seriph"
  key_links:
    - from: "style.css"
      to: ".slidev-slide"
      via: "background: #0f1117 rule"
      pattern: "background.*#0f1117"
    - from: "style.css"
      to: "Google Fonts"
      via: "@import url(https://fonts.googleapis.com/css2?family=Space+Grotesk"
      pattern: "Space\\+Grotesk"
---

<objective>
Create `style.css` at the project root with the full dark theme and typography contract, and remove `theme: seriph` from `slides.md` frontmatter.

Purpose: Establish the visual foundation for all 13 slides in one CSS file. The seriph removal prevents the light theme from fighting the dark overrides. The style.css is Slidev's auto-imported global stylesheet — every rule here applies universally with no per-slide changes needed.

Output: `style.css` (new file) with dark tokens, Space Grotesk font, groove grid styles, two-cols panel style, drum-sheet blend mode, heading colors. `slides.md` with `theme: seriph` line removed.
</objective>

<execution_context>
@/home/pigotz/Projects/drum-theory/.claude/get-shit-done/workflows/execute-plan.md
@/home/pigotz/Projects/drum-theory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/pigotz/Projects/drum-theory/.planning/PROJECT.md
@/home/pigotz/Projects/drum-theory/.planning/ROADMAP.md
@/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-CONTEXT.md
@/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-UI-SPEC.md

<interfaces>
<!-- Key facts extracted from codebase inspection -->

From slides.md (lines 1–11) — current frontmatter:
```yaml
---
theme: seriph
title: "You Can't Read This"
drawings:
  persist: false
transition: slide-left
base: /drum-theory/
timer: countdown
duration: 12min
layout: cover
---
```
The line `theme: seriph` on line 2 must be deleted. All other frontmatter lines MUST be preserved exactly.

Slide 2 uses `layout: image` with `image: /drum-sheet.png` — the PNG is served from the public/ directory.

Two-cols slides (3–7, 9, 12) render right panels as `.slidev-layout.two-cols > *:last-child`.

Groove grid is a `<table>` inside slides.md — empty cells are `<td>` elements without inline background styles.

The `style.css` file does NOT exist yet — create it fresh at the project root `/home/pigotz/Projects/drum-theory/style.css`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create style.css with dark theme, typography, layout rules</name>
  <files>style.css</files>
  <read_first>
    - `/home/pigotz/Projects/drum-theory/slides.md` — full file, understand the DOM structure (layouts used, img tag for slide 2, table structure for groove grid)
    - `/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-UI-SPEC.md` — complete design contract (tokens, typography scale, component restyle contracts)
    - `/home/pigotz/Projects/drum-theory/.planning/03-visual-redesign/03-CONTEXT.md` — all D-01 through D-14 decisions
  </read_first>
  <action>
Create `/home/pigotz/Projects/drum-theory/style.css` with the following content in this exact order:

**1. Google Fonts import (D-05):**
```css
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;700&display=swap');
```

**2. CSS custom property overrides (D-02) — in `:root {}`:**
```css
:root {
  --slidev-theme-primary: #e2e8f0;
}
```

**3. Global dark background (D-03) — on `body` and `.slidev-slide`:**
```css
body {
  background: #0f1117;
}

.slidev-slide {
  background: #0f1117;
  color: #e2e8f0;
  font-family: 'Space Grotesk', sans-serif;
}
```

**4. Typography — headings (D-06):**
```css
h1, h2, h3 {
  color: #e2e8f0;
  font-family: 'Space Grotesk', sans-serif;
}
```

**5. Two-cols right panel surface color (D-10):**
```css
.slidev-layout.two-cols > *:last-child {
  background: #1a1d27;
}
```

**6. Groove grid cells (D-11) — empty cells dark fill, all cells get border:**
```css
table td {
  background: #1a1d27;
  border: 1px solid #2d3244;
}
```

**7. Slide 2 drum-sheet.png blend mode (D-09):**
```css
.slidev-layout.image img {
  mix-blend-mode: multiply;
}
```

No `!important` in any of these rules (per D-02). No per-layout rules for `layout: end` or `layout: statement` (per D-08). No presenter-view overrides (per D-04).
  </action>
  <verify>
    <automated>grep -n "Space+Grotesk" /home/pigotz/Projects/drum-theory/style.css && grep -n "slidev-theme-primary" /home/pigotz/Projects/drum-theory/style.css && grep -n "#0f1117" /home/pigotz/Projects/drum-theory/style.css && grep -n "#1a1d27" /home/pigotz/Projects/drum-theory/style.css && grep -n "mix-blend-mode" /home/pigotz/Projects/drum-theory/style.css && grep -n "#2d3244" /home/pigotz/Projects/drum-theory/style.css</automated>
  </verify>
  <acceptance_criteria>
    - `style.css` exists at `/home/pigotz/Projects/drum-theory/style.css`
    - File contains `@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk`
    - File contains `--slidev-theme-primary: #e2e8f0`
    - File contains `background: #0f1117` (at least twice — body and .slidev-slide)
    - File contains `font-family: 'Space Grotesk', sans-serif`
    - File contains `.slidev-layout.two-cols > *:last-child` with `background: #1a1d27`
    - File contains `table td` with `background: #1a1d27` and `border: 1px solid #2d3244`
    - File contains `.slidev-layout.image img` with `mix-blend-mode: multiply`
    - File does NOT contain `!important`
    - File does NOT contain rules for `.slidev-presenter` or presenter view
  </acceptance_criteria>
  <done>style.css exists with all 7 rule blocks, no !important, no presenter overrides.</done>
</task>

<task type="auto">
  <name>Task 2: Remove theme: seriph from slides.md frontmatter</name>
  <files>slides.md</files>
  <read_first>
    - `/home/pigotz/Projects/drum-theory/slides.md` — read lines 1–15 to confirm current frontmatter before editing
  </read_first>
  <action>
Open `slides.md` and delete ONLY the line `theme: seriph` (currently line 2). All other frontmatter lines must remain exactly as-is:

After edit, the frontmatter block must be:
```yaml
---
title: "You Can't Read This"
drawings:
  persist: false
transition: slide-left
base: /drum-theory/
timer: countdown
duration: 12min
layout: cover
---
```

Do NOT add a replacement theme line. Do NOT add `theme: default`. Per D-01: start from Slidev's minimal default by simply having no `theme:` key.
  </action>
  <verify>
    <automated>grep -n "theme:" /home/pigotz/Projects/drum-theory/slides.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "theme: seriph" /home/pigotz/Projects/drum-theory/slides.md` returns no output (exit 1)
    - `grep "^title:" /home/pigotz/Projects/drum-theory/slides.md` returns `title: "You Can't Read This"`
    - `grep "^base:" /home/pigotz/Projects/drum-theory/slides.md` returns `base: /drum-theory/`
    - The `---` frontmatter delimiters on lines 1 and 11 (now line 10) remain in place
    - No other frontmatter keys have been added or removed
  </acceptance_criteria>
  <done>slides.md frontmatter has no theme: key; base: /drum-theory/ preserved; all other keys intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Fonts CDN → browser | External font load — no user data crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Google Fonts @import | accept | Font URL is public; no auth tokens or PII sent; standard web practice |
| T-03-02| Tampering | style.css CSS rule specificity | accept | Presentation-only app; no user data at risk; worst case is visual regression caught in QA |
</threat_model>

<verification>
After both tasks complete, start the Slidev dev server and verify:

1. `cd /home/pigotz/Projects/drum-theory && npx slidev slides.md --port 3030` (background)
2. Navigate to http://localhost:3030 — slide 1 (Cover) background must be dark (#0f1117)
3. Navigate to slide 2 — drum-sheet.png must blend against dark background (white areas transparent)
4. Navigate to slides 3–7 — SVG symbols must be visible (white on dark)
5. Navigate to slide 9 — right panel must show #1a1d27 surface color
6. Navigate to slide 12 — groove grid empty cells must show dark fill with borders
7. Headings throughout must use Space Grotesk font (check via browser DevTools font inspector if needed)
</verification>

<success_criteria>
- `style.css` created with all required dark theme rules
- `slides.md` has `theme: seriph` removed
- No white bleed-through on slides 1, 8, 10, 13 (full-bleed dark layouts)
- Groove grid cell borders visible (1px solid #2d3244)
- drum-sheet.png blends to dark background
- Space Grotesk font loads and applies to slide headings
</success_criteria>

<output>
After completion, create `/home/pigotz/Projects/drum-theory/.planning/phases/03-visual-redesign/03-01-SUMMARY.md`
</output>
