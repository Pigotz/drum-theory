---
phase: 01-slide-skeleton-backing-track-deploy
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - components/LoopPlayer.vue
autonomous: true
requirements:
  - AUDIO-01
  - AUDIO-02

must_haves:
  truths:
    - "LoopPlayer.vue renders a button showing '▶ Play' on initial load"
    - "Clicking the button starts the audio loop and changes button text to '⏹ Stop'"
    - "Clicking again pauses the audio and reverts button to '▶ Play'"
    - "Audio src uses import.meta.env.BASE_URL so it resolves correctly under /drum-theory/ on GitHub Pages"
    - "No autoplay — audio only starts on user click"
    - "aria-label updates with play/stop state for accessibility"
  artifacts:
    - path: "components/LoopPlayer.vue"
      provides: "Play/pause toggle component for backing track"
      exports: []
      contains: "import.meta.env.BASE_URL"
  key_links:
    - from: "components/LoopPlayer.vue"
      to: "public/backing.mp3"
      via: "import.meta.env.BASE_URL + 'backing.mp3' at runtime"
      pattern: "import.meta.env.BASE_URL"

---

<objective>
Create `components/LoopPlayer.vue` — the backing track play/pause component used on
slide 12. Uses plain HTML5 `<audio loop>` with a Vue 3 toggle ref. No Tone.js.

Purpose: The Human Drum session (slide 12) requires a drumless backing track playing on
loop. The component must be ready before slides.md is authored (Plan 04), which places
`<LoopPlayer />` inline on slide 12.

Output: `components/LoopPlayer.vue` — a working Vue 3 SFC that can be dropped into a
Slidev slide and plays/pauses a looping audio file via button click.
</objective>

<execution_context>
@/home/pigotz/Projects/drum-theory/.claude/get-shit-done/workflows/execute-plan.md
@/home/pigotz/Projects/drum-theory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/pigotz/Projects/drum-theory/.planning/01-slide-skeleton-backing-track-deploy/01-CONTEXT.md
@/home/pigotz/Projects/drum-theory/.planning/01-slide-skeleton-backing-track-deploy/01-RESEARCH.md
@/home/pigotz/Projects/drum-theory/.planning/01-slide-skeleton-backing-track-deploy/01-UI-SPEC.md
@/home/pigotz/Projects/drum-theory/CLAUDE.md

<interfaces>
<!-- Slidev/Vite globals available in component scope -->

import.meta.env.BASE_URL
  Type: string
  Value in dev: "/"
  Value in production build (--base /drum-theory/): "/drum-theory/"
  Usage: Prefix asset filenames — `import.meta.env.BASE_URL + 'backing.mp3'`

Vue 3 Composition API (available via <script setup>):
  ref(initialValue) -> Ref<T>
  .value accessor on refs

HTML5 Audio API:
  audio.value.play()   // returns Promise<void> — triggers playback (user gesture required)
  audio.value.pause()  // void — pauses playback
  <audio loop>         // attribute — restarts at end of file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoopPlayer.vue with HTML5 audio toggle</name>
  <read_first>
    - /home/pigotz/Projects/drum-theory/.planning/01-slide-skeleton-backing-track-deploy/01-RESEARCH.md (Pattern 6: LoopPlayer.vue implementation, Pitfall 5: autoplay policy, Anti-patterns: hardcoding audio path without BASE_URL)
    - /home/pigotz/Projects/drum-theory/.planning/01-slide-skeleton-backing-track-deploy/01-UI-SPEC.md (Interaction Contract: LoopPlayer, Copywriting Contract: "▶ Play" / "⏹ Stop", Color: accent #6366f1 for button only)
    - /home/pigotz/Projects/drum-theory/CLAUDE.md (audio path pattern: import.meta.env.BASE_URL + 'samples/kick.wav' — same pattern applies here)
  </read_first>
  <files>components/LoopPlayer.vue</files>
  <action>
Create `components/LoopPlayer.vue` with the following exact implementation. The
`components/` directory may already exist from the scaffold — create it if not.

```vue
<script setup>
import { ref } from 'vue'

const audio = ref(null)
const playing = ref(false)

const src = import.meta.env.BASE_URL + 'backing.mp3'

const toggle = () => {
  if (playing.value) {
    audio.value.pause()
  } else {
    audio.value.play()
  }
  playing.value = !playing.value
}
</script>

<template>
  <audio
    ref="audio"
    :src="src"
    loop
  />
  <button
    @click="toggle"
    :aria-label="playing ? 'Stop backing track' : 'Play backing track'"
    style="
      background: #6366f1;
      color: white;
      font-weight: 700;
      font-size: 14px;
      padding: 12px 16px;
      min-height: 44px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
    "
  >
    {{ playing ? '⏹ Stop' : '▶ Play' }}
  </button>
</template>
```

Key implementation rules:
- `src` is set once via `import.meta.env.BASE_URL + 'backing.mp3'` — NOT hardcoded as
  `/backing.mp3` (which 404s on GitHub Pages) and NOT using `$slidev.configs.base`
  (which adds complexity)
- `audio.value.play()` is called ONLY inside the `@click` handler — never in any
  lifecycle hook. This satisfies the browser autoplay policy (Pitfall 5)
- Do NOT import Tone.js — this component uses plain HTML5 audio (RESEARCH.md explicitly
  states the Tone.js `await Tone.start()` rule does NOT apply here)
- Button color `#6366f1` (seriph indigo accent) — per UI-SPEC color table, this is the
  ONLY element using the accent color
- `min-height: 44px` — projector pointer safety per UI-SPEC spacing notes
- `aria-label` updates dynamically: "Play backing track" when stopped, "Stop backing
  track" when playing — per UI-SPEC accessibility notes
  </action>
  <verify>
    <automated>grep -n "import.meta.env.BASE_URL" /home/pigotz/Projects/drum-theory/components/LoopPlayer.vue</automated>
  </verify>
  <acceptance_criteria>
    - `components/LoopPlayer.vue` exists
    - File contains `import.meta.env.BASE_URL + 'backing.mp3'` (grep returns a match)
    - File contains `audio.value.play()` inside a function body (not in onMounted or lifecycle)
    - File contains `loop` attribute on the `<audio>` element
    - File contains `#6366f1` as the button background color
    - File contains `min-height: 44px`
    - File contains `:aria-label="playing ? 'Stop backing track' : 'Play backing track'"`
    - File contains `{{ playing ? '⏹ Stop' : '▶ Play' }}`
    - File does NOT contain `import.*tone` or `Tone.start` (no Tone.js)
    - File does NOT contain hardcoded `src="/backing.mp3"` without BASE_URL
  </acceptance_criteria>
  <done>LoopPlayer.vue created with HTML5 audio toggle, correct BASE_URL audio path, indigo button styling, and dynamic aria-label.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → audio.play() | Browser autoplay policy enforced by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Denial of Service | audio.value.play() | accept | Browser autoplay policy already prevents unsolicited audio; play() is gated behind user click |
| T-01-03-02 | Information Disclosure | public/backing.mp3 | accept | Public asset; no sensitive content; placeholder file before live use |
</threat_model>

<verification>
After task completes:
1. `cat components/LoopPlayer.vue` — verify full implementation
2. `grep "import.meta.env.BASE_URL" components/LoopPlayer.vue` — must return match
3. `grep "Tone" components/LoopPlayer.vue` — must return nothing (no Tone.js)
4. `grep "onMounted\|onSlideEnter" components/LoopPlayer.vue` — must return nothing (no lifecycle calls to play)
</verification>

<success_criteria>
- components/LoopPlayer.vue exists with complete implementation
- Audio src uses import.meta.env.BASE_URL + 'backing.mp3'
- play() called only inside click handler — never in lifecycle
- No Tone.js import
- Button: #6366f1 background, min-height 44px, dynamic aria-label
- Button text: "▶ Play" when stopped, "⏹ Stop" when playing
</success_criteria>

<output>
After completion, create `/home/pigotz/Projects/drum-theory/.planning/phases/01-slide-skeleton-backing-track-deploy/01-03-SUMMARY.md`
</output>
